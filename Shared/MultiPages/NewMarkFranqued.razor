@inject NavigationStacker navManager;


<div class="w-full h-full flex flex-col overflow-y-auto">
    <MarkseHeader>
        <ButtonCustom>ADM</ButtonCustom>
    </MarkseHeader>
    <BackButton />

    <div class="justify-center items-center flex flex-1">
        <div class="md:w-[350px] flex flex-row flex-wrap gap-3 justify-center items-center">
            <BoxShadowGreen Size="150px" OnClick=@(() => navManager.NavigateTo("/novaMarca/novaMarca", true))>Criar Termo</BoxShadowGreen>
            <BoxShadowGreen Size="150px" OnClick=@(() => navManager.NavigateTo("/novoVendedor", true))>Novo Vendedor</BoxShadowGreen>
            <BoxShadowGreen Size="150px" OnClick=@(() => navManager.NavigateTo("/novoCliente", true))>Novo Cliente</BoxShadowGreen>
        </div>
    </div>
</div>

@code {
    /* [Parameter]
    public InputData cupom { get; set; }

    <div class="h-[400px] w-full flex flex-col items-center">
        <div class="w-[600px] flex flex-row gap-2">
            <input placeholder="Digite o cupom"
                class="shadow outline-0 border-0 bg-zinc-700 border rounded-2xl w-[400px] py-2 px-3 text-zinc-200 leading-tight focus:outline-none focus:shadow-outline" />
            <div class="w-[150px] h-1/2">
                <ButtonStyled><Loading/></ButtonStyled>
        </div>
     </div>
    </div>
    */
}